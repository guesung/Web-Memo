# dependencies
**/node_modules

# testing
**/coverage

# build
**/dist
**/build
**/dist-zip

# env
# 환경별 파일은 공개돼도 되는 값(웹 URL·NODE_ENV)만 담으므로 추적합니다.
# 비밀은 apps/web/.env(추적 제외)에 둡니다.
# 부정 패턴에 슬래시를 붙여 packages/env로 한정합니다. 슬래시가 없으면 경로 제한이
# 없어져 apps/web/.env.production 같은 동명 파일까지 추적 대상이 됩니다.
.env*
!.env.example
!/packages/env/.env.development
!/packages/env/.env.staging
!/packages/env/.env.production

# python
**/venv
**/__pycache__
**/*.pyc
**/*.pyo
**/*.egg-info

# etc
.DS_Store
.idea
**/.turbo

# compiled
chrome-extension/public/manifest.json
**/tailwind-output.css


vite.config.mts.timestamp**

# playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# next
.next
.vercel

# Turborepo
.turbo

# windsurf
.windsurfrules

# Task files
tasks.json
tasks/
prds/

stats.html
playwright-report
test-results

.serena/

*.tsbuildinfo

ios
android
.expo
*.ipa
*.aab

# worktrees
.worktrees/
# AI 리뷰 봇 자격 증명 (절대 커밋 금지 — public 레포)
*.pem
